name: Publish Yamachat Windows 1.0.83 Once

on:
  push:
    branches:
      - feature/desktop-connected-frame-1.0.83
    paths:
      - ".github/workflows/publish-windows-1.0.83-once.yml"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/desktop-connected-frame-1.0.83

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Verify release source
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $p = Get-Content desktop/package.json | ConvertFrom-Json
          if ($p.version -ne "1.0.83") { throw "Expected desktop 1.0.83" }

          $client = Get-Content desktop/desktop-client.html -Raw
          foreach ($marker in @(
            'id="ycPasswordRequestBack"',
            "Platform Admin · uživatelské účty",
            "Zabanovat účet",
            "Zprávy od Yamachatu",
            "yc-active-server-context-card",
            "ycActiveServerGlobalContext",
            "ycActiveServerConnectedFrame",
            "connected-tab"
          )) {
            if (-not $client.Contains($marker)) { throw "Missing desktop client marker: $marker" }
          }

          $runtime = Get-Content desktop/main.js -Raw
          foreach ($marker in @(
            "function createTray()",
            "new Tray(trayIconForCurrentState())",
            "createTray();",
            "if (behavior === 'tray')",
            "'build',",
            "'icon.ico'"
          )) {
            if (-not $runtime.Contains($marker)) { throw "Missing tray/runtime marker: $marker" }
          }

          if (-not (Test-Path build/installer.nsh)) { throw "Missing NSIS rollback hook" }
          ./scripts/restore-desktop-brand-assets.ps1 -Destination build

          foreach ($name in @(
            "icon.ico",
            "app-icon-source.png",
            "tray-controller-idle.png",
            "tray-controller-speaking.png",
            "tray-status-online.png",
            "tray-status-online-notify.png",
            "yamachat-logo-ui.webp"
          )) {
            if (-not (Test-Path (Join-Path build $name))) { throw "Missing Windows brand asset $name" }
          }
          Write-Host "PASS Windows 1.0.83 source, tray runtime and active-server client."
      
      - name: Prepare desktop runtime assets
        shell: pwsh
        run: |
          Copy-Item -Recurse -Force build desktop\build
          Copy-Item -Recurse -Force audio desktop\audio
          Copy-Item -Force boot-guard.js desktop\boot-guard.js

      - name: Install desktop dependencies
        working-directory: desktop
        run: npm ci

      - name: Build Windows Setup
        working-directory: desktop
        env:
          GH_TOKEN: ${{ github.token }}
        run: npm run release:win

      - name: Verify packaged runtime and assets
        shell: pwsh
        run: |
          $appRoot = "desktop/dist/win-unpacked/resources/app"
          $client = Get-Content "$appRoot/desktop-client.html" -Raw
          foreach ($marker in @(
            "yc-active-server-context-card",
            "ycActiveServerGlobalContext",
            "ycActiveServerConnectedFrame",
            "connected-tab",
            'id="ycPasswordRequestBack"'
          )) {
            if (-not $client.Contains($marker)) { throw "Packaged client missing $marker" }
          }

          $appBuild = Join-Path $appRoot "build"
          foreach ($name in @(
            "icon.ico",
            "app-icon-source.png",
            "tray-controller-idle.png",
            "tray-controller-speaking.png",
            "tray-status-online.png",
            "tray-status-online-notify.png",
            "yamachat-logo-ui.webp"
          )) {
            $asset = Join-Path $appBuild $name
            if (-not (Test-Path $asset)) { throw "Packaged app is missing $name" }
            Write-Host "PACKAGED $name $((Get-FileHash $asset -Algorithm SHA256).Hash)"
          }

          $exe = Get-ChildItem desktop/dist/Yamachat-Setup-1.0.83.exe
          $block = Get-ChildItem desktop/dist/Yamachat-Setup-1.0.83.exe.blockmap
          $latest = Get-Content desktop/dist/latest.yml -Raw
          if ($latest -notmatch "version:\s*1\.0\.82") { throw "latest.yml is not 1.0.83" }
          Write-Host "SETUP_SHA256=$((Get-FileHash $exe.FullName -Algorithm SHA256).Hash)"
          Write-Host "SETUP_SIZE=$($exe.Length)"
          Write-Host "BLOCKMAP_SIZE=$($block.Length)"

      - uses: actions/upload-artifact@v4
        with:
          name: yamachat-windows-1.0.83
          path: |
            desktop/dist/Yamachat-Setup-1.0.83.exe
            desktop/dist/Yamachat-Setup-1.0.83.exe.blockmap
            desktop/dist/latest.yml
          if-no-files-found: error

  publish:
    needs: build-windows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: yamachat-windows-1.0.83
          path: release-assets

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cat > release-notes.md <<'EOF'
          Yamachat 1.0.83

          - Aktivní server je teď výrazná karta-záložka napojená jednou souvislou konturou na serverový obsah.
          - Cyan/fialovo-růžový gradient a silnější glow dávají na první pohled jasně najevo, na kterém serveru právě jsi.
          - Při přepnutí serveru se celý spojený rám přesune k nové serverové kartě.
          - Domů, Přátelé, DM a další neserverové pohledy rám nezobrazují.
          - Zachované tray chování, Windows ikony, voice, obnova hesla a Platform Admin funkce z 1.0.82.
          EOF

          if gh release view v1.0.83 --repo yamboleeroy-ai/yamachat >/dev/null 2>&1; then
            gh release upload v1.0.83 release-assets/* --repo yamboleeroy-ai/yamachat --clobber
            gh release edit v1.0.83 --repo yamboleeroy-ai/yamachat --title "Yamachat 1.0.83" --notes-file release-notes.md --latest
          else
            gh release create v1.0.83 release-assets/* --repo yamboleeroy-ai/yamachat --title "Yamachat 1.0.83" --notes-file release-notes.md --latest
          fi
